name: Release

on:
  push:
    tags:
      - "v*"
      - "[0-9]*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v7

      - name: Setup JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build
        run: ./gradlew build -Pversion="${GITHUB_REF_NAME#v}"

      - name: Publish to Maven
        run: |
          test -n "$MAVEN_URL" && test -n "$MAVEN_USER" && test -n "$MAVEN_PASS"
          ./gradlew publish -Pversion="${GITHUB_REF_NAME#v}"
        env:
          MAVEN_URL: ${{ secrets.MAVEN_URL }}
          MAVEN_USER: ${{ secrets.MAVEN_USER }}
          MAVEN_PASS: ${{ secrets.MAVEN_PASS }}

      - name: Create GitHub release
        run: gh release create "$GITHUB_REF_NAME" build/libs/*.jar --generate-notes --verify-tag
        env:
          GH_TOKEN: ${{ github.token }}
